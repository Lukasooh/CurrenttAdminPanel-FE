import React, { useState } from 'react';
import { LoginForm } from '../features/auth/components/LoginForm';

const LoginPage = () => {
  // Your login form logic here, similar to what you had before
  return (
    <div>
      <h1>Login</h1>
      <LoginForm />
      
    </div>
  );
};

export default LoginPage;
